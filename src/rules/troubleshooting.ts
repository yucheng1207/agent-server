/**
 * 排障规则汇总（与 agent-demo 对齐，可后续从飞书等动态加载）
 */

export function getRulesSummary(): string {
  return `
- **给用户的提示**：具体以该次支付提交 code 返回的 **message** 为准（不是 code 的文案）。前端弹框内容 = 支付提交结束节点的「返回 message」。若该处无值，前端不会展示弹框，表示服务有问题，需找开发排查。
- **支付提交结束 code**：31=卡不满足当前优惠、32=达到优惠次数上限、33=活动结束、34=超出库存、35=有可用活动、87=保险优惠不可用处理。
`;
}
